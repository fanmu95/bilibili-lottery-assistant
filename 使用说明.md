# B站自动抽奖助手 · 使用说明

多账号自动参与 B 站抽奖动态：监控用户扫描、LLM 深度解析、两阶段复核、评论文案系统、私信检测。
提供 **Windows exe** 与 **Docker 镜像** 两种部署方式，二选一即可。

---

## 一、Windows exe 方式（桌面使用，最简单）

### 1. 下载
- **Release 页**（推荐）：https://github.com/fanmu95/bilibili-lottery-assistant/releases → 下载最新 `bili-lottery-assistant-win64.zip`
- 或 Actions 页 → 最新 run → Artifacts → `windows-exe`

### 2. 运行
```text
解压 zip → 得到文件夹「B站抽奖助手」
→ 双击  B站抽奖助手.exe
→ 自动启动后端并打开浏览器 http://127.0.0.1:8000
```

### 3. 首次使用
1. 打开界面 → **账号管理** → 添加账号（扫码登录，支持多账号）
2. **活动发现** → 添加/导入监控用户（或开"自动扫描"按间隔补货）
3. **活动列表** → 启动全自动，自动按最近开奖日期依次参与

### 4. 数据与备份
- 所有数据都在 **exe 旁 `data/` 文件夹**（配置、账号登录态、活动数据、日志）
- **备份 = 拷贝 `data/` 文件夹**；**迁移/换机器 = 整个文件夹拷走**
- 升级新版：只覆盖 exe 和 `_internal/`，`data/` 不动，登录态和配置全保留

### 5. 注意事项
- 端口 8000 被占用时**自动切换 8001~8019**（浏览器会打开实际端口）
- 首次运行是全新空库（独立于开发环境），需重新扫码登录账号
- 日志在 `data/app.log`（启动失败时先看这个文件）

---

## 二、Docker 方式（NAS / Linux 服务器）

> 镜像已构建为 **amd64 + arm64 双架构**（NAS 自动匹配 arm64，无需交叉编译）。

### 方式 A：docker compose（推荐）

#### ① 拉取镜像版（适合"下载镜像包安装"）

```yaml
# docker-compose.yml
services:
  bili-lottery:
    image: ghcr.io/fanmu95/bilibili-lottery-assistant:latest
    container_name: bili-lottery
    ports:
      - "8000:8000"
    volumes:
      # 数据持久化（配置/账号cookies/活动数据），升级容器不丢失
      - ./data:/app/data
    restart: unless-stopped
```

```bash
docker compose up -d
# 访问 http://<NAS_IP>:8000
```

#### ② 本地构建版（源码在自己手里时）

```yaml
# docker-compose.yml
services:
  bili-lottery:
    build: .
    image: bili-lottery-assistant:latest
    container_name: bili-lottery
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
```

```bash
docker compose up -d --build
```

### 方式 B：docker run（单行命令）

```bash
# 拉取镜像
docker pull ghcr.io/fanmu95/bilibili-lottery-assistant:latest

# 运行
docker run -d \
  --name bili-lottery \
  -p 8000:8000 \
  -v bili_data:/app/data \
  --restart unless-stopped \
  ghcr.io/fanmu95/bilibili-lottery-assistant:latest
```

### 数据持久化说明（重要）
- **必须挂载数据卷** `/app/data`（容器内数据库、账号 cookies、配置都在这里）
- 上面两种方式已配置：compose 用 `./data`（宿主机当前目录），run 用命名卷 `bili_data`
- **不挂载 = 容器一删，登录态和配置全丢**
- 备份：直接拷贝宿主机 `./data/` 目录（或 `docker cp bili-lottery:/app/data ./data_bak`）

### 更新升级
```bash
docker compose pull && docker compose up -d        # 拉取版：换新镜像
docker compose up -d --build                        # 构建版：重新构建
# 数据在挂载卷里，升级不丢
```

### ghcr.io 拉取说明
- 镜像包默认**私有**：需先 `docker login ghcr.io -u fanmu95 -p <PAT>`（PAT 需 `read:packages` 权限）
- 或在 GitHub 包设置页把镜像设为 **Public**，之后免登录直接 pull

---

## 三、常见问题

| 问题 | 解决 |
|---|---|
| exe 双击没反应 | 看 `data/app.log`；大概率端口被占已自动换端口，或等 5 秒让浏览器弹出 |
| 参与不按最近开奖 | 已修复（无日期活动排最后），确认后端为最新版本 |
| Docker 里看不到界面 | 确认端口映射与防火墙放行 8000 |
| 换机器迁移 | 拷 `data/`（exe）/ 拷挂载卷目录（Docker），登录态随行 |

---

*版本：v0.1.0 · 构建产物：Windows exe（Release 页下载）/ Docker 镜像（ghcr.io）*
